# v3 parity ledger

这份文档是 v3 的 upstream 对齐账本，不是上手教程。第一次接入请从 [README](../README.md) 和 [getting-started.md](./getting-started.md) 开始，按任务落地请看四份 how-to 页面，最后再回来查这份 ledger。

This ledger freezes parity scope from the upstream truth sources:

- `aiotieba/aiotieba/api/**/__init__.py`
- `aiotieba/aiotieba/__init__.py`

Rules used for this freeze:

- `aiotieba.api` and `aiotieba.api._protobuf` are empty package markers, so they are verified in evidence and intentionally have no ledger row.
- Package path plus exported symbol names are authoritative. Odd upstream names such as `Replys` and `get_uinfo_getUserInfo_web` remain visible in upstream identifiers, while the normalized C# surface can expose consumer-facing names such as `UserPostGroups`.
- `docs/todo.md` is a historical archive only. `docs/parity-v3.md` is the authoritative parity ledger, and the `Notes` column records whether each item was missing there, overly coarse there, or already outdated there.
- Current C# public baseline is `ITiebaClient` / `TiebaClient` with `Forums`, `Threads`, `Users`, `Admins`, `Messages`, and `Client`, plus `TiebaOptions`, DI registration, and factory entrypoints.
- Rows whose upstream names include `old`, `v1`, or other legacy wording describe intentionally retained peer families or peer-family data shapes. They stay searchable by upstream identity, but the active C# surface uses the normalized public names listed in this ledger.

## Integration lane legend

- `1. forum foundation/read`
- `2. forum follow/dislike/search/statistics`
- `3. thread read/comments/tab-map/recover-inspection`
- `4. thread write/moderation/sign family`
- `5. user/social/profile`
- `6. messaging/push/client lifecycle`
- `7. destructive cleanup/rollback verification`
- `N/A - docs parity only`

## Public naming normalization map

Parity rows stay anchored on upstream family identifiers. The `C# Surface` column uses the normalized public names only. Old public names stay in the mapping tables below so callers can still search for removed aliases, removed bridges, and renamed peer-family symbols.

### Removed aliases and removed ownership bridges

| Owning module | Upstream family anchor | Removed public name | Normalized current C# name | Category | Mapping note |
| --- | --- | --- | --- | --- | --- |
| `Forums` | `aiotieba.api.follow_forum` | `IForumModule.LikeAsync(string fname, ...)` | `IForumModule.FollowAsync(string fname, ...)` | `true-alias` | Same upstream family, old alias removed. |
| `Forums` | `aiotieba.api.unfollow_forum` | `IForumModule.UnlikeAsync(string fname, ...)` | `IForumModule.UnfollowAsync(string fname, ...)` | `true-alias` | Same upstream family, old alias removed. |
| `Messages` | `aiotieba.api.get_ats` | `IUserModule.GetAtsAsync(int pn = 1, ...)` | `IMessagesModule.GetAtsAsync(int pn = 1, ...)` | `relocation-bridge` | Inbox `@` reads belong on `Messages`. |
| `Messages` | `aiotieba.api.get_replys` | `IUserModule.GetRepliesAsync(int pn = 1, ...)` | `IMessagesModule.GetRepliesAsync(int pn = 1, ...)` | `relocation-bridge` | Reply-inbox reads belong on `Messages`. |
| `Admins` | `aiotieba.api.block` | `IUserModule.BlockAsync(ulong fid, string portrait, int day = 1, string reason = "", ...)` | `IAdminModule.BlockAsync(string fname, string portrait, int day = 1, string reason = "", ...)` | `relocation-bridge` | Admin and block work belongs on `Admins`; there is no surviving `fid` overload. |
| `Admins` | `aiotieba.api.block` | `IUserModule.BlockAsync(string fname, string portrait, int day = 1, string reason = "", ...)` | `IAdminModule.BlockAsync(string fname, string portrait, int day = 1, string reason = "", ...)` | `relocation-bridge` | Admin and block work belongs on `Admins`. |
| `Admins` | `aiotieba.api.del_bawu` | `IForumModule.DelBaWuAsync(string fname, string portrait, string baWuType, ...)` | `IAdminModule.DelBawuAsync(string fname, string portrait, BawuType bawuType, ...)` | `relocation-bridge` | Bawu removal belongs on `Admins`. |

### Renamed canonical, peer-family, and shape entries

| Owning module | Upstream family anchor | Old public name | Normalized current C# name | Category | Mapping note |
| --- | --- | --- | --- | --- | --- |
| `Forums` | `aiotieba.api.get_self_follow_forums_v1` | `IForumModule.GetSelfFollowForumsPagedAsync(int pn = 1, int rn = 20, ...)` | `IForumModule.GetSelfFollowForumsV1Async(int pn = 1, int rn = 20, ...)` | `peer-family` | Supported parallel upstream version-family API beside `GetSelfFollowForumsAsync(...)`. |
| `Forums` | `aiotieba.api.get_self_follow_forums_v1` | `SelfFollowForumsPaged` | `SelfFollowForumsV1` | `shape-variant` | Supported V1 peer-family container. |
| `Forums` | `aiotieba.api.get_self_follow_forums_v1` | `SelfFollowForumV1Item` | `SelfFollowForumV1` | `shape-variant` | Supported V1 peer-family item shape aligned to upstream `SelfFollowForumV1`. |
| `Users` | `aiotieba.api.get_blacklist` | `IUserModule.GetBlacklistPermissionsAsync(...)` | `IUserModule.GetBlacklistAsync(...)` | `peer-family` | Supported `get_blacklist` family under the shared `Blacklist` root. |
| `Users` | `aiotieba.api.set_blacklist` | `IUserModule.SetBlacklistPermissionsAsync(long userId, BlacklistType type = BlacklistType.All, ...)` | `IUserModule.SetBlacklistAsync(long userId, BlacklistType type = BlacklistType.All, ...)` | `peer-family` | Supported `set_blacklist` write family under the shared `Blacklist` root. |
| `Users` | `aiotieba.api.get_blacklist_old` | `IUserModule.GetBlacklistMutedAsync(int pn = 1, int rn = 20, ...)` | `IUserModule.GetBlacklistOldAsync(int pn = 1, int rn = 20, ...)` | `peer-family` | Supported `_old` sibling family under the shared `Blacklist` root. |
| `Users` | `aiotieba.api.add_blacklist_old` | `IUserModule.AddBlacklistMutedAsync(long userId, ...)` | `IUserModule.AddBlacklistOldAsync(long userId, ...)` | `peer-family` | Supported `_old` sibling mutation family. |
| `Users` | `aiotieba.api.del_blacklist_old` | `IUserModule.RemoveBlacklistMutedAsync(long userId, ...)` | `IUserModule.RemoveBlacklistOldAsync(long userId, ...)` | `peer-family` | Supported `_old` sibling mutation family. |
| `Users` | `aiotieba.api.get_blacklist` | `BlacklistPermissions` | `BlacklistUsers` | `shape-variant` | Current-family list shape. |
| `Users` | `aiotieba.api.get_blacklist` | `BlacklistPermission` | `BlacklistUser` | `shape-variant` | Current-family item shape. |
| `Users` | `aiotieba.api.get_blacklist_old` | `BlacklistMutedUsers` | `BlacklistOldUsers` | `shape-variant` | `_old` list shape. |
| `Users` | `aiotieba.api.get_blacklist_old` | `BlacklistMutedUser` | `BlacklistOldUser` | `shape-variant` | `_old` item shape. |
| `Users` | `aiotieba.api.get_uinfo_getuserinfo_app` | `IUserModule.GetBasicInfoAppAsync(int userId, ...)` | `IUserModule.GetUserInfoAppAsync(int userId, ...)` | `peer-family` | Supported App `user_info` peer beside `GetUserInfoWebAsync(...)`. |
| `Users` | `aiotieba.api.get_uinfo_getuserinfo_app` | `UserInfoApp` | `UserInfoGuInfoApp` | `shape-variant` | App `UserInfo_guinfo_app` DTO shape. |
| `Users` | `aiotieba.api.get_uinfo_getUserInfo_web` | `UserInfoWeb` | `UserInfoGuInfoWeb` | `shape-variant` | Web `UserInfo_guinfo_web` DTO shape. |
| `Users` | `aiotieba.api.set_nickname_old` | `IUserModule.SetNicknameLegacyAsync(string nickName, ...)` | `IUserModule.SetNicknameAsync(string nickName, ...)` | `peer-family` | Supported single-field nickname write beside `SetProfileAsync(...)`. |
| `Admins` | `aiotieba.api.add_bawu` | `IAdminModule.AddBaWuAsync(string fname, string userName, BawuType bawuType, ...)` | `IAdminModule.AddBawuAsync(string fname, string userName, BawuType bawuType, ...)` | `canonical-operation` | Canonical public casing normalized to `Bawu`. |
| `Admins` | `aiotieba.api.del_bawu` | `IAdminModule.DelBaWuAsync(string fname, string portrait, BawuType bawuType, ...)` | `IAdminModule.DelBawuAsync(string fname, string portrait, BawuType bawuType, ...)` | `canonical-operation` | Canonical public spelling keeps the upstream `del` verb while preserving the `Bawu` root. |
| `Users` | `aiotieba.api.get_user_contents.get_posts` | `UserPostss` | `UserPostGroups` | `shape-variant` | Public DTO anomaly normalized without changing the upstream family anchor. |

## Forum core / follow / dislike families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `follow_forum` | `aiotieba.api.follow_forum` | `IForumModule.FollowAsync(ulong)` / `FollowAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | The upstream `follow_forum` family remains fully represented in the parity ledger, while the normalized C# public surface keeps only the canonical `FollowAsync(...)` names after the v4 true-alias cleanup removed `LikeAsync(...)`. |
| `unfollow_forum` | `aiotieba.api.unfollow_forum` | `IForumModule.UnfollowAsync(ulong)` / `UnfollowAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | The upstream `unfollow_forum` family remains fully represented in the parity ledger, while the normalized C# public surface keeps only the canonical `UnfollowAsync(...)` names after the v4 true-alias cleanup removed `UnlikeAsync(...)`. |
| `sign_forum` | `aiotieba.api.sign_forum` | `IForumModule.SignAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `1. forum foundation/read` | `docs/todo.md` kept the row, but the new ledger is authoritative because it is anchored to the upstream export tree rather than backlog prose. |
| `get_fid` | `aiotieba.api.get_fid` | `IForumModule.GetFidAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `1. forum foundation/read` | `docs/todo.md` was directionally correct here, but it remains secondary because it does not define total scope. |
| `get_forum` (`Forum`) | `aiotieba.api.get_forum` | `IForumModule.GetForumAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `1. forum foundation/read` | `docs/todo.md` did not track this family at all, so the backlog was incomplete even for existing v2 forum coverage. |
| `get_forum_detail` (`Forum_detail`) | `aiotieba.api.get_forum_detail` | `IForumModule.GetDetailAsync(ulong)` and `IForumModule.GetDetailAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `1. forum foundation/read` | `docs/todo.md` had the rough idea, but the ledger now records the exact upstream family and both current C# overloads. |
| `dislike_forum` | `aiotieba.api.dislike_forum` | `IForumModule.DislikeAsync(ulong)` / `DislikeAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | `AioTieba4DotNet/Api/DislikeForum` plus the forum protocol/module now expose the dislike mutation directly on the v3 public surface without routing it through the TBS-only follow path. |
| `undislike_forum` | `aiotieba.api.undislike_forum` | `IForumModule.UndislikeAsync(ulong)` / `UndislikeAsync(string)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | `AioTieba4DotNet/Api/UndislikeForum` plus the forum protocol/module now expose the dislike rollback family directly on the v3 surface. |
| `get_follow_forums` (`FollowForum` / `FollowForums`) | `aiotieba.api.get_follow_forums` | `IForumModule.GetFollowForumsAsync(long userId, ...)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | `AioTieba4DotNet/Api/GetFollowForums`, `Models/Forums/FollowForum(s)`, and forum protocol/module wiring now cover the another-user follow-list family on the v3 surface. |
| `get_self_follow_forums` (`SelfFollowForum` / `SelfFollowForums`) | `aiotieba.api.get_self_follow_forums` | `IForumModule.GetSelfFollowForumsAsync(int pn = 1, int rn = 200, ...)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | `AioTieba4DotNet/Api/GetSelfFollowForums` keeps the current-account shape distinct, including `is_signed`, and the forum module/protocol expose it directly. |
| `get_self_follow_forums_v1` (`SelfFollowForumsV1` / `SelfFollowForumV1`) | `aiotieba.api.get_self_follow_forums_v1` | `IForumModule.GetSelfFollowForumsV1Async(int pn = 1, int rn = 20, ...)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | The V1 peer-family shape stays separate through `Api/GetSelfFollowForumsV1` plus dedicated `SelfFollowForumV1` / `SelfFollowForumsV1` public models rather than being collapsed into the newer signed-list model. |
| `get_dislike_forums` (`DislikeForum` / `DislikeForums`) | `aiotieba.api.get_dislike_forums` | `IForumModule.GetDislikeForumsAsync(int pn = 1, int rn = 20, ...)` | Implemented | `11` | `Task 11 forum core/follow owner` | `2. forum follow/dislike/search/statistics` | `Api/GetDislikeForums` plus dedicated `DislikeForum(s)` models now cover the dislike-list family, including its own page shape and forum statistics fields. |

## Forum discovery / search / statistics families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `get_cid` | `aiotieba.api.get_cid` | `IForumModule.GetCidAsync(string, string)` and `GetCidAsync(ulong, string)`; internal `Api/GetCid/GetCid.cs` is reused by the forum protocol and `IThreadModule.GoodAsync(...)`. | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 promotes the existing internal port onto the public forum discovery surface without duplicating the API implementation. |
| `get_images` (`Image` / `ImageBytes`) | `aiotieba.api.get_images` | `IForumModule.GetImageBytesAsync(string)`, `GetImageAsync(string)`, `GetImageByHashAsync(string, ForumImageSize)`, and `GetPortraitAsync(string, ForumImageSize)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 keeps image helpers on the forum public surface because upstream callers use them alongside forum/thread discovery flows; deterministic coverage now freezes content-type validation, image-size parsing, and empty-image semantics for invalid payloads. |
| `search_exact` (`ExactSearches`) | `aiotieba.api.search_exact` | `IForumModule.SearchExactAsync(string, ...)` and `SearchExactAsync(ulong, ...)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 now exposes exact-search paging, enum validation, empty-result containers, and equality-by-`pid` through dedicated public models. |
| `get_last_replyers` (`Thread_lp` / `Threads_lp` / `UserInfo_lp`) | `aiotieba.api.get_last_replyers` | `IForumModule.GetLastReplyersAsync(string, ...)` and `GetLastReplyersAsync(ulong, ...)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 keeps the upstream WS-preferred + HTTP-fallback behavior and normalizes `current_page=0` to `1` when the page size is non-zero. |
| `get_member_users` (`MemberUser` / `MemberUsers`) | `aiotieba.api.get_member_users` | `IForumModule.GetMemberUsersAsync(string, ...)` and `GetMemberUsersAsync(ulong, ...)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | HTML parsing stays aligned with the upstream member-list shape, including pagination defaults and STOKEN fail-fast behavior in the forum protocol. |
| `get_rank_forums` (`RankForum` / `RankForums`) | `aiotieba.api.get_rank_forums` | `IForumModule.GetRankForumsAsync(string, ...)` and `GetRankForumsAsync(ulong, ...)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 keeps the upstream HTML rank page shape, including inferred `HasBaWu` semantics from the rendered CSS/content markers. |
| `get_recom_status` (`RecomStatus`) | `aiotieba.api.get_recom_status` | `IForumModule.GetRecomStatusAsync(string)` and `GetRecomStatusAsync(ulong)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 maps the upstream JSON keys `total_recommend_num` and `used_recommend_num` onto a dedicated public model. |
| `get_square_forums` (`SquareForum` / `SquareForums`) | `aiotieba.api.get_square_forums` | `IForumModule.GetSquareForumsAsync(string cname, ...)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 preserves the WS-preferred + HTTP-fallback transport shape and maps upstream `is_like` onto the public `IsFollowed` property. |
| `get_statistics` (`Statistics`) | `aiotieba.api.get_statistics` | `IForumModule.GetStatisticsAsync(string)` and `GetStatisticsAsync(ulong)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 keeps the upstream ordered time-series payload as old-to-new public series rather than flattening it into a lossy aggregate shape. |
| `get_forum_level` (`LevelInfo`) | `aiotieba.api.get_forum_level` | `IForumModule.GetForumLevelAsync(string)` and `GetForumLevelAsync(ulong)` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | Task 12 keeps the upstream bootstrap flow by loading self-info prerequisites before issuing the level-info request. |
| `get_roomlist_by_fid` (`RoomList`) | `aiotieba.api.get_roomlist_by_fid` | `IForumModule.GetRoomListByFidAsync(ulong fid, ...)` via `ForumProtocol.GetRoomListByFidAsync(...)`, `Api/GetRoomListByFid/GetRoomListByFid.cs`, and `Models/Forums/RoomList.cs` | Implemented | `12` | `Task 12 forum discovery/statistics owner` | `2. forum follow/dislike/search/statistics` | The v3 forum surface now exposes the authenticated room-list family directly, and focused deterministic coverage freezes both the authenticated form packing and the flattening of `data.list[*].room_list[*]` into the public `RoomList` container instead of inventing a richer chatroom model. |

## Bawu / admin / appeals / block-management families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `add_bawu` | `aiotieba.api.add_bawu` | `IAdminModule.AddBawuAsync(string fname, string userName, BawuType bawuType, ...)` via `AdminProtocol.AddBawuAsync(...)` and `Api/AddBaWu/AddBaWu.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | The canonical admin surface keeps the public `Bawu` root directly aligned to upstream `add_bawu` while leaving the internal upstream-searchable `Api/AddBaWu` family unchanged. |
| `add_bawu_blacklist` | `aiotieba.api.add_bawu_blacklist` | `IAdminModule.AddBawuBlacklistAsync(string fname, long userId, ...)` via `AdminProtocol.AddBawuBlacklistAsync(...)` and `Api/AddBawuBlacklist/AddBawuBlacklist.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu-blacklist add operations on `Admins`, preserving the upstream `errno` / `errmsg` response shape and HTTP form endpoint. |
| `del_bawu` | `aiotieba.api.del_bawu` | `IAdminModule.DelBawuAsync(string fname, string portrait, BawuType bawuType, ...)` via `AdminProtocol.DelBawuAsync(...)` and `Api/DelBawu/DelBaWu.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | The canonical admin surface now owns bawu removal exclusively, while the public `Bawu` root stays directly traceable to upstream `del_bawu` and the old forum-side bridge remains removed. |
| `del_bawu_blacklist` | `aiotieba.api.del_bawu_blacklist` | `IAdminModule.DelBawuBlacklistAsync(string fname, long userId, ...)` via `AdminProtocol.DelBawuBlacklistAsync(...)` and `Api/DelBawuBlacklist/DelBawuBlacklist.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu-blacklist removal on `Admins`, preserving the upstream `list[]` form packing and `errno` / `errmsg` response shape. |
| `get_bawu_blacklist` (`BawuBlacklistUser` / `BawuBlacklistUsers`) | `aiotieba.api.get_bawu_blacklist` | `IAdminModule.GetBawuBlacklistAsync(string fname, int pn = 1, ...)` via `AdminProtocol.GetBawuBlacklistAsync(...)` and `Api/GetBawuBlacklist/GetBawuBlacklist.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu blacklist reads on the dedicated `Admins` public module, including HTML list parsing and page metadata mapping. |
| `get_bawu_info` (`BawuInfo` / `UserInfo_bawu`) | `aiotieba.api.get_bawu_info` | `IAdminModule.GetBawuInfoAsync(string fname, ...)` via `AdminProtocol.GetBawuInfoAsync(...)` and `Api/GetBawuInfo/GetBawuInfo.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu-team info on the dedicated `Admins` public module and keeps the upstream WebSocket-preferred + HTTP-fallback transport shape. |
| `get_bawu_perm` (`BawuPerm`) | `aiotieba.api.get_bawu_perm` | `IAdminModule.GetBawuPermAsync(string fname, string portrait, ...)` via `AdminProtocol.GetBawuPermAsync(...)` and `Api/GetBawuPerm/GetBawuPerm.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu permission reads on `Admins`, preserving the upstream `no` / `error` JSON shape and flag mapping. |
| `get_bawu_postlogs` (`Postlog` / `Postlogs`) | `aiotieba.api.get_bawu_postlogs` | `IAdminModule.GetBawuPostLogsAsync(string fname, BawuPostLogQueryOptions? options = null, ...)` via `AdminProtocol.GetBawuPostLogsAsync(...)` and `Api/GetBawuPostlogs/GetBawuPostlogs.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu post-log reads on `Admins`, including filter packing (`op_type`, `svalue`, `stype`, `begin`, `end`) and HTML/media mapping aligned with upstream behavior. |
| `get_bawu_userlogs` (`Userlog` / `Userlogs`) | `aiotieba.api.get_bawu_userlogs` | `IAdminModule.GetBawuUserLogsAsync(string fname, BawuUserLogQueryOptions? options = null, ...)` via `AdminProtocol.GetBawuUserLogsAsync(...)` and `Api/GetBawuUserlogs/GetBawuUserlogs.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu user-log reads on `Admins`, including search-type normalization and duration/page parsing from the upstream HTML shape. |
| `set_bawu_perm` | `aiotieba.api.set_bawu_perm` | `IAdminModule.SetBawuPermAsync(string fname, string portrait, BawuPermType permissions, ...)` via `AdminProtocol.SetBawuPermAsync(...)` and `Api/SetBawuPerm/SetBawuPerm.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes bawu permission writes on `Admins`, preserving the upstream permission-order JSON payload (`4,5,3,2`) and current auth-only transport gate. |
| `get_unblock_appeals` (`Appeal` / `Appeals`) | `aiotieba.api.get_unblock_appeals` | `IAdminModule.GetUnblockAppealsAsync(string fname, int pn = 1, int rn = 20, ...)` via `AdminProtocol.GetUnblockAppealsAsync(...)` and `Api/GetUnblockAppeals/GetUnblockAppeals.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes unblock-appeal reads on `Admins`, preserving the upstream form payload and JSON appeal-list mapping. |
| `handle_unblock_appeals` | `aiotieba.api.handle_unblock_appeals` | `IAdminModule.HandleUnblockAppealsAsync(string fname, IReadOnlyList<long> appealIds, bool refuse = false, ...)` via `AdminProtocol.HandleUnblockAppealsAsync(...)` and `Api/HandleUnblockAppeals/HandleUnblockAppeals.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes unblock-appeal handling on `Admins`, preserving the indexed `appeal_list[i]` form packing and upstream `status` mapping (`1` accept / `2` refuse). |
| `get_blocks` (`Block` / `Blocks`) | `aiotieba.api.get_blocks` | `IAdminModule.GetBlocksAsync(string fname, string userName = "", int pn = 1, ...)` via `AdminProtocol.GetBlocksAsync(...)` and `Api/GetBlocks/GetBlocks.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes block-list reads on `Admins`, preserving the upstream JSON + embedded-HTML page/content shape. |
| `block` | `aiotieba.api.block` | `IAdminModule.BlockAsync(string fname, string portrait, int day = 1, string reason = "", ...)` via `AdminProtocol.BlockAsync(...)` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Block-management now stays exclusively on `Admins`; the old `Users.BlockAsync(...)` bridge has been removed from the public surface. |
| `unblock` | `aiotieba.api.unblock` | `IAdminModule.UnblockAsync(string fname, long userId, ...)` via `AdminProtocol.UnblockAsync(...)` and `Api/Unblock/Unblock.cs` | Implemented | `13` | `Task 13 bawu/admin owner` | `7. destructive cleanup/rollback verification` | Task 13 now exposes unblock on `Admins`, preserving the upstream `block_un=-` / `block_uid` form payload and auth+TBS gate. |

## Thread read / recover / tab-map / media families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `get_threads` (`ShareThread` / `Thread` / `Threads` / `UserInfo_t`) | `aiotieba.api.get_threads` | `IThreadModule.GetThreadsAsync(string, ...)` and `IThreadModule.GetThreadsAsync(ulong, ...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | `docs/todo.md` marked this done, but the ledger records the exact upstream family and current overload coverage. |
| `get_posts` (`Comment_p` / `Post` / `Posts` / `Thread_p` / `UserInfo_p` / `UserInfo_pt`) | `aiotieba.api.get_posts` | `IThreadModule.GetPostsAsync(...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | `docs/todo.md` marked this done; the ledger keeps the exact upstream export family and comment-related payload types. |
| `get_comments` (`Comment` / `Comments` / `Post_c` / `Thread_c` / `UserInfo_c` / `UserInfo_cp` / `UserInfo_ct`) | `aiotieba.api.get_comments` | `IThreadModule.GetCommentsAsync(...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | `docs/todo.md` marked this done; the ledger anchors it to the upstream comments family instead of the old prose bucket. |
| `get_recovers` (`Recover` / `Recovers`) | `aiotieba.api.get_recovers` | `IThreadModule.GetRecoversAsync(string, ...)` and `GetRecoversAsync(ulong, ...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | Task 14 now exposes the authenticated recover-list read family directly on `Threads`, including upstream paging and optional deleted-author user-id filtering. |
| `get_recover_info` (`RecoverInfo`) | `aiotieba.api.get_recover_info` | `IThreadModule.GetRecoverInfoAsync(string, long tid, long pid = 0, ...)` and `GetRecoverInfoAsync(ulong, long tid, long pid = 0, ...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | Although the current upstream client facade does not publicly promote this helper, the v3 Task 14 surface now exposes the same authenticated recover-detail lookup on the C# thread module with thread/post semantics aligned to the underlying API. |
| `recover` | `aiotieba.api.recover` | `IThreadModule.RecoverAsync(string fname, long tid = 0, long pid = 0, bool isHide = false, ...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | `docs/todo.md` still showed this unchecked even though v2 already exposes `RecoverAsync`, so the backlog was stale. |
| `get_tab_map` (`TabMap`) | `aiotieba.api.get_tab_map` | `IThreadModule.GetTabMapAsync(string, ...)` and `GetTabMapAsync(ulong, ...)` | Implemented | `14` | `Task 14 thread read/recover owner` | `3. thread read/comments/tab-map/recover-inspection` | Task 14 keeps `Threads.TabDictionary` on thread-list reads for compatibility and adds a standalone authenticated `GetTabMapAsync(...)` port for the distinct upstream family. |

## Thread write / moderation / sign families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `add_post` | `aiotieba.api.add_post` | `IThreadModule.AddPostAsync(string fname, long tid, string content, string? showName = null, ...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` marked this done; the ledger keeps the exact upstream write family and current optional `showName` mapping. |
| `agree` | `aiotieba.api.agree` | `IThreadModule.AgreeAsync(...)`, `DisagreeAsync(...)`, `UnagreeAsync(...)`, and `UndisagreeAsync(...)` all route through the same upstream family. | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` split `agree` and `unagree` conceptually, but the upstream truth is one `agree` family with flags; the old backlog was therefore too coarse. |
| `del_post` | `aiotieba.api.del_post` | `IThreadModule.DelPostAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` marked this done; the ledger retains the exact upstream family. |
| `del_posts` | `aiotieba.api.del_posts` | `IThreadModule.DelPostsAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `DelPostsAsync`, so the backlog was stale. |
| `del_thread` | `aiotieba.api.del_thread` | `IThreadModule.DelThreadAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` marked this done; the ledger preserves the exact upstream family. |
| `del_threads` | `aiotieba.api.del_threads` | `IThreadModule.DelThreadsAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `DelThreadsAsync`, so the backlog was stale. |
| `good` | `aiotieba.api.good` | `IThreadModule.GoodAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `GoodAsync`, so the backlog was stale. |
| `ungood` | `aiotieba.api.ungood` | `IThreadModule.UngoodAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `UngoodAsync`, so the backlog was stale. |
| `top` | `aiotieba.api.top` | `IThreadModule.TopAsync(...)` and `UntopAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed `top` unchecked, and it did not model the paired undo behavior already present in v2. |
| `move` | `aiotieba.api.move` | `IThreadModule.MoveAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `MoveAsync`, so the backlog was stale. |
| `recommend` | `aiotieba.api.recommend` | `IThreadModule.RecommendAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `RecommendAsync`, so the backlog was stale. |
| `set_thread_privacy` | `aiotieba.api.set_thread_privacy` | `IThreadModule.SetThreadPrivacyAsync(...)` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | `docs/todo.md` still showed this unchecked even though v2 already exposes `SetThreadPrivacyAsync`, so the backlog was stale. |
| `sign_forums` | `aiotieba.api.sign_forums` | `IForumModule.SignForumsAsync(...)` via `ForumProtocol.SignForumsAsync(...)` and `Api/SignForums/SignForums.cs` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | Task 15 now exposes the bulk-sign family on the forum module and keeps the upstream hybrid web-form request shape for `/c/c/forum/msign`, including the `Subapp-Type: hybrid` header. |
| `sign_growth` | `aiotieba.api.sign_growth` | `IForumModule.SignGrowthAsync(...)` via `ForumProtocol.SignGrowthAsync(...)` and `Api/SignGrowth/SignGrowth.cs` | Implemented | `15` | `Task 15 thread write/moderation owner` | `4. thread write/moderation/sign family` | Task 15 now exposes the growth-sign family separately from `sign_forum` / `sign_forums` and keeps the upstream web task-pack path `/mo/q/usergrowth/commitUGTaskInfo` with `act_type=page_sign`. |

## User / social / profile / legacy families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `add_blacklist_old` | `aiotieba.api.add_blacklist_old` | `IUserModule.AddBlacklistOldAsync(long userId, ...)` via `UserProtocol.AddBlacklistOldAsync(...)` and `Api/AddBlacklistOld/AddBlacklistOld.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The `_old` blacklist add mutation stays public as a supported sibling family rather than being merged into the `SetBlacklistAsync(...)` surface. |
| `del_blacklist_old` | `aiotieba.api.del_blacklist_old` | `IUserModule.RemoveBlacklistOldAsync(long userId, ...)` via `UserProtocol.RemoveBlacklistOldAsync(...)` and `Api/DelBlacklistOld/DelBlacklistOld.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The delete-half of the `_old` blacklist flow remains a first-class sibling family so callers can keep the upstream add/remove semantics separate from permissions writes. |
| `follow_user` | `aiotieba.api.follow_user` | `IUserModule.FollowAsync(string portrait, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` only tracked the friendly verb `follow`, so it was already too coarse to act as the authoritative parity ledger. |
| `unfollow_user` | `aiotieba.api.unfollow_user` | `IUserModule.UnfollowAsync(string portrait, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` only tracked the friendly verb `unfollow`, so the backlog name did not preserve the exact upstream package identity. |
| `get_ats` (`At` / `Ats`) | `aiotieba.api.get_ats` | `IMessagesModule.GetAtsAsync(int pn = 1, ...)` via `MessagesProtocol.GetAtsAsync(...)` and `Api/GetAts/GetAts.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | Inbox `@` reads now stay exclusively on `Messages`; the old `Users.GetAtsAsync(...)` bridge has been removed from the public surface. |
| `get_blacklist` (`BlacklistUser` / `BlacklistUsers`) | `aiotieba.api.get_blacklist` | `IUserModule.GetBlacklistAsync(...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The `get_blacklist` family stays distinct and uses the shared `Blacklist` root so it does not overlap conceptually with the `_old` sibling family. |
| `get_blacklist_old` (`BlacklistOldUser` / `BlacklistOldUsers`) | `aiotieba.api.get_blacklist_old` | `IUserModule.GetBlacklistOldAsync(int pn = 1, int rn = 20, ...)` via `UserProtocol.GetBlacklistOldAsync(...)` and `Api/GetBlacklistOld/GetBlacklistOld.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The `_old` blacklist read family keeps its own page/model shape and WS-preferred + HTTP-fallback transport behavior instead of being collapsed into `GetBlacklistAsync(...)`. |
| `get_fans` (`Fan` / `Fans`) | `aiotieba.api.get_fans` | `IUserModule.GetFansAsync(long userId, int pn = 1, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` still showed this unchecked even though v2 already exposes `GetFansAsync`, so the backlog was stale. |
| `get_follows` (`Follow` / `Follows`) | `aiotieba.api.get_follows` | `IUserModule.GetFollowsAsync(long userId, int pn = 1, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` marked this done, but the ledger now preserves the exact upstream package identity. |
| `get_rank_users` (`RankUser` / `RankUsers`) | `aiotieba.api.get_rank_users` | `IUserModule.GetRankUsersAsync(string fname, int pn = 1, ...)` via `UserProtocol.GetRankUsersAsync(...)` and `Api/GetRankUsers/GetRankUsers.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The v3 user module now exposes the upstream HTML rank-users family directly, with dedicated `RankUser` / `RankUsers` public models and deterministic coverage for row/page parsing. |
| `get_replys` (`Reply` / `Replys`) | `aiotieba.api.get_replys` | `IMessagesModule.GetRepliesAsync(int pn = 1, ...)` via `MessagesProtocol.GetRepliesAsync(...)` and `Api/GetReplys/GetReplys.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | Reply-inbox reads now stay exclusively on `Messages`; the old `Users.GetRepliesAsync(...)` bridge has been removed even though the upstream family keeps the odd `Replys` spelling. |
| `get_selfinfo_initNickname` | `aiotieba.api.get_selfinfo_initNickname` | `IUserModule.GetSelfInfoInitNicknameAsync(...)` via `UserProtocol.GetSelfInfoInitNicknameAsync(...)` and `Api/GetSelfInfoInitNickname/GetSelfInfoInitNickname.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The v3 user surface now keeps the direct compatibility family available alongside the merged `GetSelfInfoAsync()` convenience entrypoint. |
| `get_selfinfo_moindex` (`UserInfo_moindex`) | `aiotieba.api.get_selfinfo_moindex` | `IUserModule.GetSelfInfoMoIndexAsync(...)` via `UserProtocol.GetSelfInfoMoIndexAsync(...)` and `Api/GetSelfInfoMoIndex/GetSelfInfoMoIndex.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The direct moindex family is now surfaced separately instead of existing only as an internal merge input. |
| `get_uinfo_getuserinfo_app` (`UserInfo_guinfo_app`) | `aiotieba.api.get_uinfo_getuserinfo_app` | `IUserModule.GetUserInfoAppAsync(int userId, ...)` via `UserProtocol.GetUserInfoAppAsync(...)`, `Api/GetUInfoGetUserInfoApp/GetUInfoGetUserInfoApp.cs`, and public `UserInfoGuInfoApp` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The App `user_info` read stays public under the shared `GetUserInfo` root while the exported DTO name now stays visibly aligned to upstream `UserInfo_guinfo_app`. |
| `get_uinfo_getUserInfo_web` (`UserInfo_guinfo_web`) | `aiotieba.api.get_uinfo_getUserInfo_web` | `IUserModule.GetUserInfoWebAsync(int userId, ...)` via `UserProtocol.GetUserInfoWebAsync(...)`, `Api/GetUInfoGetUserInfoWeb/GetUInfoGetUserInfoWeb.cs`, and public `UserInfoGuInfoWeb` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The Web `user_info` family remains a first-class peer to `GetUserInfoAppAsync(...)`, and its exported DTO now stays visibly aligned to upstream `UserInfo_guinfo_web`. |
| `get_uinfo_panel` (`UserInfo_panel`) | `aiotieba.api.get_uinfo_panel` | `IUserModule.GetPanelInfoAsync(string nameOrPortrait, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` marked this done; the ledger retains the exact upstream family. |
| `get_uinfo_user_json` (`UserInfo_json`) | `aiotieba.api.get_uinfo_user_json` | `IUserModule.GetUserInfoJsonAsync(string username, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` marked this done; the ledger retains the exact upstream family. |
| `get_user_contents` (`get_posts` / `get_threads` / `UserPost` / `UserPosts` / `UserPostss` / `UserThread` / `UserThreads` / `CMD`) | `aiotieba.api.get_user_contents` | `IUserModule.GetPostsAsync(...)`, `IUserModule.GetThreadsAsync(...)`, public models `UserPost` / `UserPosts` / `UserPostGroups` / `UserThread` / `UserThreads`, plus root helper `UserContent.Cmd` and `IUserModule.UserContentCmd` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The public C# DTO anomaly is normalized to `UserPostGroups`, while the underlying upstream family and internal request naming stay traceable to `UserPostss`. |
| `get_user_contents.get_posts` | `aiotieba.api.get_user_contents.get_posts` | `IUserModule.GetPostsAsync(int userId, uint pn = 1, uint rn = 20, string version = "8.9.8.5", ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` tracked this as `get_user_posts`, which was helpful but not authoritative naming. |
| `get_user_contents.get_threads` | `aiotieba.api.get_user_contents.get_threads` | `IUserModule.GetThreadsAsync(int userId, uint pn = 1, bool publicOnly = true, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` tracked this as `get_user_threads`, which was helpful but not authoritative naming. |
| `get_user_forum_info` (`UserForumInfo` / `UserInfo_uf`) | `aiotieba.api.get_user_forum_info` | `IUserModule.GetUserForumInfoAsync(ulong fid, string portrait, ...)` and `GetUserForumInfoAsync(string fname, string portrait, ...)` via `UserProtocol.GetUserForumInfoAsync(...)` and `Api/GetUserForumInfo/GetUserForumInfo.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The user module now exposes the authenticated forum-local user-info family with both fid and forum-name overloads; deterministic coverage freezes auth preflight, fid resolution, and JSON mapping. |
| `login` | `aiotieba.api.login` | `IUserModule.LoginAsync(...)` via `UserProtocol.LoginAsync(...)`, `Api/Login/Login.cs`, and public `Models/Users/LoginResult.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The compatibility login family now returns both user info and TBS on the public surface and updates session TBS only after success, matching the existing v3 session lifecycle rules. |
| `profile` (`get_homepage` / `get_uinfo_profile` / `Homepage` / `Thread_pf` / `UserInfo_pf` / `CMD`) | `aiotieba.api.profile` | `IUserModule.GetProfileAsync(int)` / `GetProfileAsync(string)` cover `profile.get_uinfo_profile`, and `IUserModule.GetHomepageAsync(int userId, int pn = 1, ...)` covers `profile.get_homepage`. | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The nested profile families stay explicitly separate on the public surface: `GetProfileAsync(...)` reads profile metadata, while `GetHomepageAsync(...)` reads homepage content plus the homepage owner snapshot. |
| `profile.get_homepage` | `aiotieba.api.profile.get_homepage` | `IUserModule.GetHomepageAsync(int userId, int pn = 1, ...)` via `UserProtocol.GetHomepageAsync(...)` and `Api/Profile/GetHomepage/GetHomepage.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The homepage family now preserves the upstream WS-preferred + HTTP-fallback behavior and maps the owner snapshot separately from the returned thread list. |
| `profile.get_uinfo_profile` | `aiotieba.api.profile.get_uinfo_profile` | `IUserModule.GetProfileAsync(int)` and `IUserModule.GetProfileAsync(string)` via `AioTieba4DotNet.Api.Profile.GetUInfoProfile.GetUInfoProfile<T>` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` tracked this only as a coarse `get_profile` item rather than the exact upstream nested family. |
| `remove_fan` | `aiotieba.api.remove_fan` | `IUserModule.RemoveFanAsync(long userId, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | `docs/todo.md` still showed this unchecked even though v2 already exposes `RemoveFanAsync`, so the backlog was stale. |
| `set_blacklist` | `aiotieba.api.set_blacklist` | `IUserModule.SetBlacklistAsync(long userId, BlacklistType type = BlacklistType.All, ...)` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The `set_blacklist` write family remains separate from the `_old` add/remove sibling family so callers can choose the correct transport and semantics explicitly. |
| `set_nickname_old` | `aiotieba.api.set_nickname_old` | `IUserModule.SetNicknameAsync(string nickName, ...)` via `UserProtocol.SetNicknameAsync(...)` and `Api/SetNicknameOld/SetNicknameOld.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The single-field nickname mutation remains public as its own write family under the normalized `SetNicknameAsync(...)` name, while `SetProfileAsync(...)` continues to own the broader profile-metadata update path. |
| `set_profile` | `aiotieba.api.set_profile` | `IUserModule.SetProfileAsync(string nickName, string sign, Gender gender, ...)` via `UserProtocol.SetProfileAsync(...)` and `Api/SetProfile/SetProfile.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The current profile-mutation family is implemented separately from `set_nickname_old`, preserving the recommended-vs-legacy distinction for profile writes instead of flattening both into one method. |
| `tieba_uid2user_info` (`UserInfo_TUid`) | `aiotieba.api.tieba_uid2user_info` | `IUserModule.GetUserByTiebaUidAsync(long tiebaUid, ...)` via `UserProtocol.GetUserByTiebaUidAsync(...)` and `Api/TiebaUid2UserInfo/TiebaUid2UserInfo.cs` | Implemented | `16` | `Task 16 user/social/profile owner` | `5. user/social/profile` | The tieba-uid lookup family now preserves the upstream WS-preferred + HTTP-fallback transport shape and exposes a dedicated `UserInfoTUid` model rather than overloading the app/web basic-info models. |

## Messaging / push / client lifecycle families

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `get_group_msg` (`UserInfo_ws` / `WsMessage` / `WsMsgGroup` / `WsMsgGroups`) | `aiotieba.api.get_group_msg` | `IMessagesModule.GetGroupMessagesAsync(...)` via `MessagesProtocol`, `Api/GetGroupMsg/GetGroupMsg.cs`, and `Models/Messages/*` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 adds the dedicated `Messages` module, cursor initialization from websocket bootstrap, and explicit `WsMessage` / `WsMsgGroup` public models instead of leaving websocket messaging hidden behind the lifecycle module. |
| `init_websocket` (`WsMsgGroupInfo`) | `aiotieba.api.init_websocket` | Lifecycle `IClientModule.InitWebSocketAsync(...)` plus internal `Api/InitWebSocket/InitWebSocket.cs` / `Models/Messages/WsMsgGroupInfo` consumed by `MessagesProtocol` cursor bootstrap | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 keeps `init_websocket` lifecycle-only on `Client` while still porting the upstream response family so the new `Messages` module can initialize message-group cursors without blurring the public boundary. |
| `init_z_id` | `aiotieba.api.init_z_id` | `IClientModule.InitZIdAsync(...)` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | `docs/todo.md` marked this done; the ledger retains the exact upstream lifecycle family. |
| `push_notify` (`WsNotify`) | `aiotieba.api.push_notify` | `IMessagesModule.ParsePushNotifications(byte[] payload)` via `Api/PushNotify/PushNotify.cs` and `Models/Messages/WsNotify.cs` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 keeps upstream semantics explicit: `push_notify` is surfaced as a parse-only entrypoint on `Messages`, not as a speculative event bus or background subscription framework. |
| `send_chatroom_msg` | `aiotieba.api.send_chatroom_msg` | `IMessagesModule.SendChatroomMessageAsync(...)` via `MessagesProtocol` and internal `Transport/Chatrooms/BlcpChatroomSender.cs` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 ports the BLCP-backed chatroom send path as a minimal internal transport sibling while keeping the public business surface on `Messages` and preserving safe live-fixture gating for verification. |
| `send_msg` | `aiotieba.api.send_msg` | `IMessagesModule.SendMessageAsync(long, ...)` / `SendMessageAsync(string, ...)` via `MessagesProtocol` and `Api/SendMsg/SendMsg.cs` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 adds the websocket private-message send family on `Messages`, including portrait-or-username resolution through the existing user panel lookup path. |
| `set_msg_readed` | `aiotieba.api.set_msg_readed` | `IMessagesModule.SetMessageReadAsync(WsMessage, ...)` via `MessagesProtocol` and `Api/SetMsgReaded/SetMsgReaded.cs` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | Task 17 ports the websocket read-state family and reuses the initialized private-message group cursor state instead of inventing a second read-tracking mechanism. |
| `sync` | `aiotieba.api.sync` | `IClientModule.SyncAsync(...)` | Implemented | `17` | `Task 17 messaging/client owner` | `6. messaging/push/client lifecycle` | `docs/todo.md` marked this done; the ledger retains the exact upstream lifecycle family. |

## Support / export families from the upstream API tree

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `_classdef` (`Account`, `TypeMessage`, `Containers`, `Frag*`, `TypeFrag*`, `UserInfo`, `VoteInfo`) | `aiotieba.api._classdef` | Public support models are exposed across `AioTieba4DotNet.Models.*`, `AioTieba4DotNet.Models.Shared`, `AioTieba4DotNet.Models.Messages.MessageEnums.cs`, and `AioTieba4DotNet.Contracts.Account` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | v3 keeps these support exports as public root/contracts/models types rather than reproducing the upstream package layout; the remaining missing support pieces are now covered by `FragUnknown`, public `Account`, and the websocket message/group enums. |

## Top-level `aiotieba` package exports affecting parity scope

| Feature | Upstream Module | C# Surface | Status | Target Task | Coverage Owner | Integration Lane | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Top-level `Client` facade | `aiotieba.__init__ -> Client` | `TiebaClient`, `ITiebaClient`, `DependencyInjection.AddAioTiebaClient(...)`, `ITiebaClientFactory`, and `TiebaClientFactory` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | `docs/modules.md` now documents the direct, DI, and factory entrypoints together, so the root-client contract matches the actual v3 surface. |
| Top-level timeout configuration | `aiotieba.__init__ -> TimeoutConfig` | Public `TimeoutConfig` plus `TiebaOptions.Timeout`, `TiebaOptions.RequestTimeout`, and `TiebaOptions.MaxReadRetryAttempts` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | The standalone timeout export is now real and wired to the existing v3 execution policy instead of being a documentation-only alias. |
| Top-level `Account` export | `aiotieba.__init__ -> Account` | Public `AioTieba4DotNet.Contracts.Account` plus `TiebaClient(Account)` and `ITiebaClientFactory.CreateClient(Account)` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | The root account export now maps cleanly onto the existing client/factory creation paths instead of exposing the internal session account type. |
| Top-level exception family | `aiotieba.__init__ -> exception` | Public root `AioTieba4DotNet.*` exceptions (`TiebaException`, `TiebaAuthenticationException`, `TieBaServerException`, etc.) | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | `docs/todo.md` never tracked exception-surface parity, so the backlog was incomplete as a product contract reference. |
| Top-level wildcard enums export | `aiotieba.__init__ -> from .enums import *` | Public enum surface under `AioTieba4DotNet.Models*`, including `Gender`, privacy enums, search/rank/admin enums, `BlacklistType`, and websocket `WsStatus` / `GroupType` / `MsgType` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | The previously missing websocket/message enums are now public, which closes the remaining top-level enum-export parity gap without inventing a separate wildcard namespace. |
| Top-level logging helpers | `aiotieba.__init__ -> enable_filelog, get_logger, logging` | Root `TiebaLogging.EnableFileLog(...)`, `TiebaLogging.GetLogger(...)`, and `TiebaLogging.Factory` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | v3 now keeps a minimal root logging helper for parity while still remaining compatible with normal Microsoft.Extensions.Logging host integration. |
| Top-level version export | `aiotieba.__init__ -> __version__` | Root `VersionInfo.Version` | Implemented | `20` | `Task 20 parity/docs owner` | `N/A - docs parity only` | The runtime version is now publicly exported from the existing assembly informational version value instead of living only in package metadata and release docs. |
