name: Publish to NuGet

on:
    push:
        tags:
            - 'v*.*.*'  # 监听以 v 开头的标签推送

jobs:
    verify:
        name: Verify release lane (${{ matrix.framework }})
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                framework: [ net8.0, net9.0, net10.0 ]

        steps:
            -   name: Checkout code
                uses: actions/checkout@v6

            -   name: Setup .NET SDK
                uses: actions/setup-dotnet@v5
                with:
                    dotnet-version: |
                        8.x
                        9.x
                        10.x

            -   name: Restore dependencies
                run: dotnet restore AioTieba4DotNet.sln --nologo

            -   name: Build solution
                run: dotnet build AioTieba4DotNet.sln --configuration Release --no-restore --nologo

            -   name: Run deterministic tests for ${{ matrix.framework }}
                run: dotnet test AioTieba4DotNet.Tests/AioTieba4DotNet.Tests.csproj --configuration Release --no-build -f ${{ matrix.framework }} --filter "TestCategory!=Integration&TestCategory!=Live" --nologo

    generator-consistency:
        name: Verify generated protobuf outputs
        runs-on: ubuntu-latest

        steps:
            -   name: Checkout code
                uses: actions/checkout@v6

            -   name: Setup .NET SDK
                uses: actions/setup-dotnet@v5
                with:
                    dotnet-version: |
                        8.x
                        9.x
                        10.x

            -   name: Restore dependencies
                run: dotnet restore AioTieba4DotNet.sln --nologo

            -   name: Regenerate protobuf outputs
                run: dotnet run --project ProtoGenerator/ProtoGenerator.csproj

            -   name: Check generator consistency
                shell: bash
                run: |
                    if [[ -n "$(git status --short --untracked-files=all -- AioTieba4DotNet)" ]]; then
                        git status --short --untracked-files=all -- AioTieba4DotNet
                        echo "Generated protobuf outputs are out of date."
                        exit 1
                    fi

    migration-docs:
        name: Verify migration docs exist
        runs-on: ubuntu-latest

        steps:
            -   name: Checkout code
                uses: actions/checkout@v6

            -   name: Check release migration artifacts
                shell: bash
                run: |
                    required_files=(
                        docs/migration-v1-to-v2.md
                        docs/release-notes-v2.md
                    )

                    missing=0
                    for file in "${required_files[@]}"; do
                        if [[ ! -f "$file" ]]; then
                            echo "Missing required release artifact: $file"
                            missing=1
                        fi
                    done

                    if (( missing != 0 )); then
                        exit 1
                    fi

                    printf 'Found release artifacts:\n'
                    printf ' - %s\n' "${required_files[@]}"

    publish:
        # `.github/workflows/publish.yml` is part of the external nuget.org trusted publishing contract.
        # If this workflow file is renamed, the nuget.org trusted publishing policy must be updated to match `publish.yml`.
        runs-on: ubuntu-latest
        needs:
            - verify
            - generator-consistency
            - migration-docs
        permissions:
            contents: write
            id-token: write

        steps:
            -   name: Checkout code
                uses: actions/checkout@v6
                with:
                    fetch-depth: 0

            -   name: Setup .NET SDK
                uses: actions/setup-dotnet@v5
                with:
                    dotnet-version: |
                        8.x
                        9.x
                        10.x

            -   name: Preflight release tag version
                shell: bash
                run: |
                    VERSION="${GITHUB_REF_NAME#v}"

                    if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-(preview|rc)\.[0-9]+)?$ ]]; then
                        echo "Release tag '$GITHUB_REF_NAME' produced VERSION='$VERSION'."
                        echo "Expected vX.Y.Z, vX.Y.Z-preview.N, or vX.Y.Z-rc.N so only GA, preview, and rc releases are published."
                        exit 1
                    fi

                    echo "VERSION=$VERSION" >> "$GITHUB_ENV"

            -   name: Determine release channel
                shell: bash
                run: |
                    if [[ "${VERSION}" =~ -(preview|rc)\.[0-9]+$ ]]; then
                        echo "PRERELEASE=true" >> $GITHUB_ENV
                    else
                        echo "PRERELEASE=false" >> $GITHUB_ENV
                    fi

            -   name: Restore dependencies
                run: dotnet restore --nologo

            -   name: Build the project
                run: dotnet build --configuration Release --no-restore -p:Version=${{ env.VERSION }} --nologo

            -   name: Pack the project
                run: dotnet pack --configuration Release --no-build --output ./nupkg -p:Version=${{ env.VERSION }} --nologo

            -   name: Validate packed artifacts
                shell: bash
                run: bash ./.github/scripts/validate-package-artifacts.sh "./nupkg" "AioTieba4DotNet.${{ env.VERSION }}"

            -   name: Login to NuGet
                id: login
                uses: NuGet/login@v1

            -   name: Push to NuGet
                run: dotnet nuget push ./nupkg/*.nupkg --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" --source https://api.nuget.org/v3/index.json --skip-duplicate

            -   name: Create Release
                uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b
                with:
                    files: |
                        ./nupkg/*.nupkg
                        ./nupkg/*.snupkg
                    generate_release_notes: true
                    draft: false
                    prerelease: ${{ env.PRERELEASE == 'true' }}
                env:
                    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
